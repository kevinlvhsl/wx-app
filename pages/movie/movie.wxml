<view class="page-bg">
  <view class="section-search">
    <input class="mv-search" bindinput="bindKeyInput"
      placeholder="电影名称或导演名" maxlength="20" auto-focus />
    <icon type="search" size="26" bindtap="serachMov"/>
  </view>
  <view class="ms-wrap">
    <view class="mov" wx:for="{{in_theaters.subjects}}" wx:for-item="item"  wx:key="{{item.id}}"
      bindtap="goMovieDetail" data-id="{{item.id}}">
      <image class="mov-avatar" src="{{item.images.small}}" background-size="cover"></image>
      <text class="mov-title" >{{item.title}}</text>
    </view>
  </view>
</view>
